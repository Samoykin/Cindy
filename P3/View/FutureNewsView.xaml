<UserControl x:Class="P3.View.FutureNewsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" LostFocus="UserControl_LostFocus" LostMouseCapture="UserControl_LostMouseCapture" 
             >
    <Grid>
        <ListView ItemsSource="{Binding}" Width="100" Height="150">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Value}" TextWrapping="Wrap"/>
                        <Line Stroke="#FFD6DDFD" X2="200" Y1="1" Y2="1" X1="10" StrokeThickness="2"></Line>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
